<tr id="agenda_expedient_<%= expedient.id %>" class="hover:bg-gray-50 transition"> 
  <td class="px-6 py-4 font-medium"><%= expedient.file_number %></td>
  <td class="text-center"><%= expedient.subject&.name || 'Sin tema' %> (<%= (expedient.subject.priority) %>) </td>
  <% if expedient.daily_agenda.hcd? %>
    <td class="text-center"><%= expedient.destination&.name || 'Sin destino' %></td>
    <td class="text-center"><%= expedient.responsible %></td>
  <% else %>
    <td class="text-center"><%= expedient.opinion %></td>
  <% end %>
  <% if expedient.treated? %>
    <td class="text-center"><%= expedient.treat_date.to_date %></td>
  <% end %>
  <td class="px-6 py-4 text-center space-x-2">
    <%= link_to "Ver",
      expedient_path(expedient),
      data: { turbo_frame: "modal" },
      class: "text-blue-600 hover:underline text-gray-600" %>
    <%= link_to "Quitar",
      delete_from_agenda_modal_expedient_path(expedient, page: params[:page]),
      data: { turbo_frame: 'modal' },
      class: "text-blue-600 hover:underline text-gray-600" %>
  </td>
</tr>
